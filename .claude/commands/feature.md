---
description: Run the feature development chain (brainstorm → plan → implement → test → review → verify → docs)
---

Invoke the `feature` skill (`/Users/dev/code/sharp.arena/.claude/skills/feature/SKILL.md`) and execute its full chain for the feature request below.

Feature request: $ARGUMENTS
